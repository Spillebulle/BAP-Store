//! TODO: the helper. Reads a plan on stdin, validates against a closed list,
//! runs it, streams events on stdout.

fn main() {
    eprintln!("bap-helper is not built yet.");
    std::process::exit(2);
}
